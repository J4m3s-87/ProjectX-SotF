================================================================================
                         PROJECT X - MODULE REFERENCE GUIDE
                              Master Mod v1.7 (Client)
================================================================================

This document provides detailed technical information about each module in the
Project X Master Mod, including HOW TO USE each feature, configurable values,
internal mechanics, and in-game effects.

Last Updated: March 08, 2026

================================================================================
                                TABLE OF CONTENTS
================================================================================

 1.  Player Module
 2.  Stack Module
 3.  Zipline Module
 4.  Relocator Module
 5.  Structure Durability Module
 6.  Water Collectors Module
 7.  Meat Dryer Module
 8.  Scary Cross Module
 9.  Faster Crafting Module
 10. RaidCustomizer Module
 11. Loot Respawn Module
 12. Dedicated Superuser Module
 13. Building Cheats Module
 14. AmmoUI Module  *** UPDATED ***
 15. Builder Stacks Module  *** NEW ***
 16. Discord Bridge Module
 17. GUI Layout & Menu System
 18. Audio Control Module  *** NEW ***

Removed Modules (no longer in build):
 - Open Sesame (HarmonyPatchAll vtable corruption)
 - Prefab Repair (HarmonyPatchAll vtable corruption)
 - Stone Gate (ClassInjector + inventory crash)

================================================================================
                              1. PLAYER MODULE
================================================================================

PURPOSE:
Provides player quality-of-life features including teleportation of companions.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ 1. Open the Project X menu (default: Insert key)                            │
│    NOTE: Menu is blocked when the Esc pause menu is open.                    │
│ 2. Navigate to the "Player" tab                                             │
│ 3. Toggle cheats on/off:                                                    │
│    • God Mode - Makes you invulnerable to all damage                        │
│    • Infinite Stamina - Never run out of energy                             │
│    • No Hunger - Hunger meter stays full                                    │
│    • No Thirst - Thirst meter stays full                                    │
│    • No Sleep - No tiredness                                                │
│    • Infinite Ammo - Weapons never need reloading                           │
│    • No Fall Damage - Survive any height                                    │
│    • Infinite Logs - Carry unlimited logs                                   │
│    • Infinite Stones - Carry unlimited stones                               │
│    • AI Ghost Player - AI ignores you                                       │
│    • Fill Inventory - Fill all items to max                                 │
│    • Max Strength - Sets player strength to max level (toggle)              │
│    • No World Gravity - Disable world gravity (uses debug console)         │
│                                                                              │
│ COMPANIONS (Teleport tab → "COMPANIONS" section):                            │
│ • Unstuck Kelvin / Unstuck Virginia — teleport 2m in front of you           │
│ • Revive Kelvin / Revive Virginia — revive via debug console                │
│   (removedead + addcharacter robby/virginia)                                │
│ • Virginia Visit — trigger Virginia to visit you (Virginiavisit)            │
│ • Happy Virginia — set Virginia sentiment to max (virginiasentiment 100)    │
│ • Enable Stat Overrides — master toggle for HP/damage multipliers          │
│ • Kelvin HP Multi — HP multiplier slider (1x–200x)                         │
│ • Virginia HP Multi — HP multiplier slider (1x–200x)                       │
│                                                                              │
│ HP multipliers apply:                                                        │
│  - On first-aid revival (VailActor.Revive postfix, 1s delayed)              │
│  - On actor spawn/load (VailActor.OnActorEnabled postfix)                   │
│  - Sliders auto-enable Stat Overrides when moved above 1.0                 │
│                                                                              │
│ MOVEMENT:                                                                    │
│ • Walk Speed, Run Speed, Swim Speed, Jump Multiplier sliders               │
│ • Reset Movement toggle — resets all to vanilla defaults (1.0)             │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS (Settings → Mods → ProjectX - Cheats):
- GodMode (bool): Enable invulnerability
- IsInfStamina (bool): Enable infinite stamina
- IsNoHungry (bool): Disable hunger
- IsNoDehydration (bool): Disable dehydration
- IsNoSleep (bool): Disable sleep requirement
- IsInfiniteAmmo (bool): Unlimited ammunition
- IsNoFallDamage (bool): Disable fall damage
- InfiniteLogs (bool): Unlimited log carrying
- InfiniteStones (bool): Unlimited stone carrying
- InfiniteInventory (bool): Set ALL items to 999,999,999

MOVEMENT OPTIONS (Settings → Mods → ProjectX - Movement):
- IsNoClip (bool): Fly mode
- NoClipSpeed (float, 0.5-20): Flight speed
- NoClipUpDownSpeed (float, 0.1-10): Vertical flight speed
- WalkSpeed (float, 0.1-10): Walk speed multiplier
- RunSpeed (float, 0.1-10): Run speed multiplier
- SwimSpeed (float, 0.1-10): Swim speed multiplier
- JumpMultiplier (float, 0.5-10): Jump height multiplier
- IsNoGravity (bool): Float when jumping

INTERNAL MECHANICS:
- Kelvin internal name: "Robby", "Robby0", "Robby1"...
- Virginia internal name: "Virginia0", "Virginia1"...
- Uses VailActor.SetPositionAndRotation() for teleportation

================================================================================
                              2. STACK MODULE
================================================================================

PURPOSE:
Modifies the maximum stack size for all items in the game, allowing for
greater inventory capacity.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ QUICK MODE - INFINITE STACKS:                                               │
│ 1. Open the Project X menu (Insert)                                         │
│ 2. Toggle "Infinite Inventory" ON                                           │
│ 3. All items now stack to 999,999,999                                       │
│                                                                              │
│ CUSTOM MODE - PER-ITEM STACKS:                                              │
│ 1. Go to game Settings → Mods → Project X                                   │
│ 2. Find the "Stack: [Category]" sections                                    │
│ 3. Enter your desired max stack for each item type                          │
│ 4. Changes apply immediately                                                │
│                                                                              │
│ EXAMPLES:                                                                    │
│ • Set MaxStickStack to "100" = Carry 100 sticks per slot                   │
│ • Set MaxAmmoStack to "500" = All ammo types stack to 500                  │
│ • Set MaxMeatStack to "50" = Carry 50 meat per slot                        │
└──────────────────────────────────────────────────────────────────────────────┘

ITEM CATEGORIES & IDs:
┌──────────────────────────────────────────────────────────────────────────────┐
│ CRAFTING ITEMS                                                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 419 = Duct Tape          │ ID 502 = Cloth              │ ID 418 = Wire    │
│ ID 527 = Batteries          │ ID 403 = Rope               │ ID 416 = Board   │
│ ID 420 = C4                 │ ID 414 = Vodka              │ ID 410 = Watch   │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ STICKS & ROCKS                                                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 392 = Stick              │ ID 393 = Rock               │ ID 476 = Small Rock│
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ PRINTING ITEMS                                                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 390 = Resin              │ ID 553 = Tech Mesh          │ ID 560 = Hook    │
│ ID 657 = GPS Case           │                             │                  │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ ELECTRIC ITEMS                                                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 634 = Solar Panel        │ ID 661 = Golfcart Battery   │ ID 635 = Bulb    │
│ ID 590 = Radio              │                             │                  │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ MEDICATION                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 437 = Meds               │ ID 455 = Health Mix         │ ID 456 = Health+ │
│ ID 461 = Energy Mix         │ ID 462 = Energy Mix+        │                  │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ FOOD & DRINKS                                                                │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 517 = Cooking Pot        │ ID 433 = Meat               │ ID 436 = Fish    │
│ ID 438 = MRE                │ ID 441 = Energy Bar         │ ID 439 = Energy Drink│
│ ID 434 = Canned Food        │ ID 464 = Cat Food           │ ID 421 = Ramen   │
│ ID 425 = Crunchie           │ ID 466 = Oyster             │ ID 401 = Egg     │
│ ID 570 = Steak Bite         │ ID 571 = Bacon Bite         │ ID 569 = Brain Bite│
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ ANIMAL DROPS                                                                 │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 506 = Shell              │ ID 472 = Hide               │ ID 479 = Feather │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ THROWABLES                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 388 = Molotov            │ ID 381 = Grenade            │ ID 417 = Sticky Bomb│
│ ID 474 = Spear              │ ID 440 = Flare              │                  │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ BODY PARTS & BONES                                                           │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 482 = Head               │ ID 480 = Arm                │ ID 481 = Leg     │
│ ID 430 = Skull              │ ID 405 = Bone               │                  │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ ARMOR                                                                        │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 593 = Creepy Armor       │ ID 494 = Bone Armor         │ ID 727 = Solafite│
│ ID 554 = Tech Armor         │ ID 473 = Leaf Armor         │ ID 519 = Hide Armor│
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ AMMO (All Types)                                                             │
├──────────────────────────────────────────────────────────────────────────────┤
│ IDs: 646, 648, 644, 650, 647, 651, 642, 643, 652, 362, 363, 364, 369, 387   │
│ ID 507 = Stone Arrow        │ ID 618 = Printed Arrow      │ ID 373 = Carbon Arrow│
│ ID 368 = Crossbow Bolt      │ ID 523 = Zipline Rope       │                  │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ PLANTS & SEEDS                                                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 484 = Leaf               │                             │                  │
│ Plant IDs: 451, 454, 595, 445, 465, 449, 453, 400, 594, 450, 399, 398, etc. │
│ Seed IDs: 596, 454, 598, 599, 605, 600, 601, 602, 603, 604, 605, 606, 597   │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ MISCELLANEOUS                                                                │
├──────────────────────────────────────────────────────────────────────────────┤
│ ID 664 = Solafite           │ ID 508 = Skin Pouch         │ ID 626 = Hang Glider│
│ ID 504 = Tarp               │ ID 469 = Air Tank           │ ID 518 = Paper Target│
│ ID 524 = Golf Ball          │ ID 496 = Cash               │                  │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS:
- InfiniteInventory (bool): Set all items to max stack (999,999,999)
- MaxTapeStack, MaxClothStack, etc.: Per-item stack values (string, parsed as int)

================================================================================
                              3. ZIPLINE MODULE
================================================================================

PURPOSE:
Extends zipline construction distance and rope gun firing range.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ This module works AUTOMATICALLY once configured.                             │
│                                                                              │
│ CONFIGURE ZIPLINE DISTANCES:                                                 │
│ 1. Go to Settings → Mods → Project X                                       │
│ 2. Find the "Zipline" section                                               │
│ 3. Set your preferred values:                                               │
│    • MaxZipLineLength: How long the zipline rope can be                     │
│    • MaxRopeBridgeLength: How far rope bridges can span                     │
│    • MaxShootingDistance: How far the rope gun can shoot                    │
│                                                                              │
│ IN-GAME USAGE:                                                               │
│ 1. Equip the Rope Gun                                                       │
│ 2. Aim at a distant anchor point                                            │
│ 3. The extended range lets you shoot much farther than vanilla!             │
│ 4. Build rope bridges spanning valleys that were previously impossible      │
│                                                                              │
│ DEFAULT VALUES:                                                              │
│ • Vanilla zipline length: ~40m                                              │
│ • Modded zipline length: 200m (5x farther!)                                │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS:
- MaxZipLineLength (float): Maximum zipline rope length
  Default: 200m
  
- MaxRopeBridgeLength (float): Maximum rope bridge construction length
  Default: 200m
  Note: Internally divided by 400 for API (e.g., 200m = 0.5 internal)
  
- MaxShootingDistance (float): Rope gun maximum firing range
  Default: 200m

INTERNAL MECHANICS:
- Modifies RopeGunController._maxRopeLength
- Modifies RopeGunController._maxFiringRange
- Modifies ConstructionSettings.Targeting.MaxRopeBridgeLength
- Uses polling pattern (non-Harmony) for IL2CPP compatibility

================================================================================
                              4. RELOCATOR MODULE
================================================================================

PURPOSE:
Allows structures to be picked up and relocated without being destroyed.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ This module works AUTOMATICALLY - no extra steps needed!                     │
│                                                                              │
│ TO RELOCATE A STRUCTURE:                                                     │
│ 1. Approach any built structure (traps, furniture, decorations, etc.)       │
│ 2. Look at it and hold the "Relocation" key (check game controls)           │
│ 3. The structure becomes moveable - place it in the new location            │
│ 4. Confirm placement                                                        │
│                                                                              │
│ WHAT CAN BE RELOCATED:                                                       │
│ • Traps (deadfall traps, etc.)                                              │
│ • Utility items (drying racks, etc.)                                        │
│ • Furniture (beds, chairs, benches)                                         │
│ • Decorations (skull lamps, etc.)                                           │
│ • Shelters (log cabins, tents)                                              │
│                                                                              │
│ NOTE: In vanilla game, many structures can only be destroyed when moved.    │
│ This mod lets you pick them up and reposition them instead!                 │
└──────────────────────────────────────────────────────────────────────────────┘

CATEGORY TYPES:
┌───────────────────────────────────────────────────────────────┐
│ Category ID │ Name       │ Examples                          │
├─────────────┼────────────┼───────────────────────────────────┤
│ 0           │ Traps      │ Deadfall traps                    │
│ 1           │ Utility    │ Meat dryer, drying rack           │
│ 2           │ Furniture  │ Beds, chairs                      │
│ 3           │ Decoration │ Skull lamp                        │
│ 4           │ Shelters   │ Log cabin                         │
└───────────────────────────────────────────────────────────────┘

CONFIG OPTIONS:
- RelocatorEnabled (bool): Enable/disable module

INTERNAL MECHANICS:
- Sets StructureRecipe._relocateMode = 0 (allow relocation)
- Backs up original modes per recipe ID for restoration
- Client-only module (#if !SERVER)

================================================================================
                         5. STRUCTURE DURABILITY MODULE
================================================================================

PURPOSE:
Multiplies structure health for increased durability.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ This module works AUTOMATICALLY once configured.                             │
│                                                                              │
│ TO CONFIGURE:                                                                │
│ 1. Go to Settings → Mods → Project X                                       │
│ 2. Find "StructureDurabilityMultiplier"                                     │
│ 3. Set your desired multiplier:                                             │
│    • 1.0 = Normal HP (vanilla)                                              │
│    • 2.0 = Double HP (structures take twice as long to destroy)            │
│    • 5.0 = 5x HP (very tanky structures)                                   │
│    • 0.5 = Half HP (easier to destroy - for challenge runs)                │
│                                                                              │
│ EFFECT:                                                                      │
│ All structures you build will have multiplied health.                       │
│ Enemies will need more hits to break your walls!                            │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS:
- StructureDurabilityMultiplier (float): HP multiplier
  Default: 1.0 (no change)
  Example: 2.0 = double HP

INTERNAL MECHANICS:
- Harmony Postfix patch on StructureDestructionManager.GetStructureInfo()
- Multiplies both StructureInfo.MaxHealth and StructureInfo.Health

================================================================================
                          6. WATER COLLECTORS MODULE
================================================================================

PURPOSE:
Prevents rain catchers from freezing in winter when near a fire source.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ This module works AUTOMATICALLY in winter.                                   │
│                                                                              │
│ TO KEEP RAIN CATCHERS UNFROZEN IN WINTER:                                   │
│ 1. Build a Rain Catcher as normal                                           │
│ 2. Build a FIRE nearby (standing fire, campfire, etc.)                      │
│ 3. Keep the fire LIT                                                        │
│ 4. As long as the catcher is within the heat radius, it won't freeze!      │
│                                                                              │
│ CONFIGURING HEAT RADIUS:                                                     │
│ 1. Go to Settings → Mods → Project X                                       │
│ 2. Find "WaterCollectorHeatRadius"                                          │
│ 3. Set the detection distance (default: 10 meters)                          │
│                                                                              │
│ TIPS:                                                                        │
│ • Place multiple catchers around one central fire                           │
│ • Make sure the fire stays burning!                                         │
│ • The effect only matters in Winter (Season 3)                              │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS:
- WaterCollectorHeatRadius (float): Detection radius for fire sources
  Default: 10m

FIRE DETECTION (Updated Feb 08 2026):
- Harmony Postfix on TemperatureModifierVolume.OnEnable() tracks all heat
  sources in a static list (Physics.OverlapSphere is stripped by IL2CPP)
- Uses Vector3.Distance checks against tracked heat sources
- Searches for objects with tag prefix "Fire" (covers all fire variants)
- State-cached: SetFrozen() only called on state transitions, not every tick

INTERNAL MECHANICS:
- Harmony Postfix on RainCatcher.Attached() to track all catchers
- OnGuiTick() piggybacked on ProjectXGUI.OnGUI() for scanning loop
- Uses reflection MethodInfo for SetFrozen(bool) on RainCatcher
- HashSet<int> tracks unfrozen catcher instance IDs (state caching)
- Heat source list cleanup every ~60s (null reference pruning)
- Calls RainCatcher.SetFrozen(false) when fire nearby in winter
- Calls RainCatcher.SetFrozen(true) when no fire nearby in winter

================================================================================
                             7. MEAT DRYER MODULE
================================================================================

PURPOSE:
Modifies drying rack behavior based on season and proximity to heat sources.
Meat dries faster when near a fire, with speed varying by season.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ This module works AUTOMATICALLY on all drying racks.                         │
│                                                                              │
│ TO MAXIMIZE MEAT DRYING SPEED:                                               │
│ 1. Build a Drying Rack                                                      │
│ 2. Place meat/fish on the rack                                              │
│ 3. Build a FIRE nearby to create a "temperature volume"                     │
│ 4. The drying speed bonus is applied automatically!                         │
│                                                                              │
│ SEASON EFFECTS:                                                              │
│ ┌─────────┬──────────────┬───────────────────────────────────┐             │
│ │ Season  │ Drying Boost │ Min Temp Required                 │             │
│ ├─────────┼──────────────┼───────────────────────────────────┤             │
│ │ Spring  │ 30x faster   │ 20°C (easy - small fire works)   │             │
│ │ Summer  │ 25x faster   │ 15°C (easiest - any fire works)  │             │
│ │ Autumn  │ 15x faster   │ 30°C (medium - need good fire)   │             │
│ │ Winter  │ 5x faster    │ 50°C (hard - need big fire!)     │             │
│ └─────────┴──────────────┴───────────────────────────────────┘             │
│                                                                              │
│ CONFIGURING CHECK INTERVAL:                                                  │
│ Settings → Mods → Project X → MeatDryerProximityCheckInterval               │
│ Default: 5 seconds (how often the mod checks temperature)                   │
│                                                                              │
│ TIPS:                                                                        │
│ • Summer is the BEST season for drying (low temp requirement, high boost)  │
│ • Winter is the WORST (need a huge fire for minimal boost)                 │
│ • Place racks CLOSE to fires for best results                              │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS:
- MeatDryerProximityCheckInterval (float): How often to check temperature
  Default: 5.0 seconds

INTERNAL MECHANICS:
- Recipe ID 19 = Drying Rack Prefab
- Season Values: 0=Spring, 1=Summer, 2=Autumn, 3=Winter
- Injects MeatDryerController component into Drying Rack prefab
- Modifies DryingRackHookPoint._isInsideTemperatureVolume
- Modifies DryingRackHookPoint._dryBoostMultiplier
- Modifies DryingRackHookPoint._minTemperatureToGainDryBoost
- Reads SeasonsManager._activeSeason via cached reflection

================================================================================
                            8. SCARY CROSS MODULE
================================================================================

PURPOSE:
Transforms the buildable Cross structure into a functional demon-repelling
effigy that scares away enemies when powered and can even burn them.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ BUILDING A SCARY CROSS:                                                      │
│ 1. Build a Cross structure (found in building menu)                         │
│ 2. Connect it to ELECTRICITY (solar panels + batteries)                     │
│ 3. The cross is now active!                                                 │
│                                                                              │
│ HOW IT WORKS:                                                                │
│ • When powered, the cross has a "demon detection" radius                    │
│ • When demons/cannibals enter the radius, the lights glow brighter          │
│ • The more enemies nearby, the hotter the cross gets                        │
│ • At high heat, the cross CATCHES FIRE and scares/burns enemies            │
│ • CAUTION: The fire damages the cross structure over time!                  │
│                                                                              │
│ EFFECT PROGRESSION:                                                          │
│ 1. Demons approach → Lights start glowing                                   │
│ 2. More demons → Lights get brighter, temperature rises                     │
│ 3. Temperature threshold → Cross catches fire!                              │
│ 4. Fire scares demons away (effigy effect)                                  │
│ 5. Fire can BURN demons within range                                        │
│ 6. Continued fire → Cross takes damage and eventually breaks               │
│                                                                              │
│ TIPS:                                                                        │
│ • Place crosses at your base perimeter for protection                       │
│ • Keep spare materials to rebuild burned crosses                            │
│ • Multiple crosses = better coverage                                        │
│ • Works best with consistent power supply                                   │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS (Advanced - Settings → Mods → Project X):
┌──────────────────────────────────────────────────────────────────────────────┐
│ Setting                    │ Purpose                                         │
├────────────────────────────┼─────────────────────────────────────────────────┤
│ SC_TempRiseThreshold       │ Light intensity needed to start heating         │
│ SC_BurnThreshold           │ Temperature to start burning                    │
│ SC_DamageThreshold         │ Temperature to start taking damage              │
│ SC_DamageAfterSec          │ Seconds between damage ticks                    │
│ SC_LightIntensityRise      │ Light intensity increase per second             │
│ SC_LightIntensityReduce    │ Light intensity decrease per second             │
│ SC_TempRisePerSec          │ Temperature increase per second                 │
│ SC_TempReducePerSec        │ Temperature decrease per second                 │
│ SC_EffigyMinRange          │ Minimum effigy scare range                      │
│ SC_EffigyMaxRange          │ Maximum effigy scare range                      │
│ SC_EffigyMinStrength       │ Minimum effigy scare strength                   │
│ SC_EffigyMaxStrength       │ Maximum effigy scare strength                   │
│ SC_EffigyDisabledRange     │ Effigy range when unpowered                     │
│ SC_EffigyDisabledStrength  │ Effigy strength when unpowered                  │
│ SC_BurnDemonRangeMin       │ Minimum demon burn range                        │
│ SC_BurnDemonRangeMax       │ Maximum demon burn range                        │
│ SC_BurnDemonTimeConfig     │ Burn trigger duration                           │
│ SC_DemonDetectRadiusMin    │ Minimum demon detection radius                  │
│ SC_DemonDetectRadiusMax    │ Maximum demon detection radius                  │
└──────────────────────────────────────────────────────────────────────────────┘

INTERNAL MECHANICS:
- Recipe ID 71 = Cross Recipe
- Item ID 468 = Cross (Held)
- Profile ID 403 = Standing Fire (for fire effects)
- Injects MakeCrossScary component
- Uses DemonDetector component for enemy detection
- Lightbulb intensity range: 4096 to 32768 (scaled by heat)
- Lightbulb range: 6 to 24 meters (scaled by heat)

DAMAGE SCALING:
Based on structure HP, lightbulbs fail progressively:
- HP 1: 8 bulbs out
- HP 2: 6 bulbs out
- HP 3: 5 bulbs out
- HP 4: 4 bulbs out
- HP 5: 2 bulbs out

================================================================================
                    9. FASTER CRAFTING MODULE  *** SUSPENDED ***
================================================================================

⚠ STATUS: SUSPENDED — Removed from main build (Feb 08 2026)

PURPOSE:
Speeds up backpack crafting animations, reducing the time required to craft
items in the inventory screen.

WHY SUSPENDED:
Harmony patches on CraftingCog caused Fatal CLR Error / vtable corruption
when opening the backpack (Tab key). Multiple approaches attempted:
  - Harmony Prefix on CraftingCog.Update() → crash
  - Direct field access on CraftingCog._craftingSpeed → crash
  - Property setter CraftingCog.CraftingSpeed → crash
  - Polling-based approach → IL2CPP strips FindObjectOfType for CraftingCog
All paths lead to IL2CPP vtable corruption on this specific type.
The standalone FasterCrafting.dll may work in isolation but is incompatible
with the monolithic Master DLL architecture.

ORIGINAL CONFIG OPTIONS (for reference):
- FasterCraftingEnabled (bool): Enable/disable faster crafting
  Default: true
- CraftingSpeedMultiplier (float): Speed multiplier
  Default: 5.0, Range: 1.0-10.0

================================================================================
                         10. RAIDCUSTOMIZER MODULE
================================================================================

PURPOSE:
Comprehensive raid and enemy customization module. Controls raid scheduling,
enemy spawn counts, stat multipliers for all enemy types, and multiplayer
scaling. Includes raid announcement system with on-screen messages. Also
adjusts follower (Kelvin/Virginia) health with persistent HP multipliers
that survive first-aid revival.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ All settings are in Settings → Mods → ProjectX - X Raids.                   │
│ Changes take effect on the next raid cycle.                                  │
│                                                                              │
│ RAID ANNOUNCEMENTS:                                                          │
│ • XR_AnnounceIncomingSearchParties: Toggle on-screen raid announcements      │
│   When enabled, incoming raids display a message showing raid name,          │
│   type (SearchParty/Creepy), and enemy count (e.g. 7-10 enemies)            │
│ • XR_PlaySoundWhenAnnounced: Toggle announcement sound (WIP)                │
│ • Announcements are triggered via Harmony Postfix on VailWorldEvents.Run... │
│   Event when a SearchPartyEvent is successfully dispatched                   │
│                                                                              │
│ RAID SCHEDULING:                                                             │
│ • XR_RaidsPerDay: How many raids per in-game day                            │
│ • XR_RaidDistribution: How raids are spread across the day                  │
│ • XR_ConsiderCurrentTime: Factor current time into scheduling               │
│ • Time-of-day toggles: Morning / Day / Evening / Night                      │
│                                                                              │
│ ENEMY SPAWN CONTROL:                                                         │
│ • XR_MinSpawnFactor / XR_MaxSpawnFactor: Spawn count range                  │
│ • XR_EnemyLimit: Maximum simultaneous enemies                                │
│ • XR_NormalCooldown: Seconds between normal raids                            │
│ • XR_IgnoreDayLimit: Raids happen regardless of game day                     │
│ • XR_IgnoreAngerLimit: Raids happen regardless of anger level                │
│                                                                              │
│ BOSS RAIDS:                                                                  │
│ • XR_BossCount: Number of bosses per boss raid                               │
│ • XR_BossCooldown: Seconds between boss raids                                │
│ • XR_BossIgnoreDayLimit / XR_BossIgnoreAngerLimit: Override limits           │
│                                                                              │
│ ENEMY STAT MULTIPLIERS (applied via VailActor.OnEnable Harmony patch):      │
│ • XR_StatMultiplierEnabled: Master toggle for stat changes                   │
│ • XR_OverrideHealthOnLoad: Override HP on world load                         │
│                                                                              │
│ ┌────────────────┬──────────────────┬──────────────────┐                   │
│ │ Enemy Type     │ Health           │ Damage           │                   │
│ ├────────────────┼──────────────────┼──────────────────┤                   │
│ │ Cannibals      │ XR_CannibalHealth│ XR_CannibalDamage│                   │
│ │ Creepies       │ XR_CreepHealth   │ XR_CreepDamage   │                   │
│ │ Bosses         │ XR_BossHealth    │ XR_BossDamage    │                   │
│ └────────────────┴──────────────────┴──────────────────┘                   │
│ Each type also has an Aggression multiplier.                                 │
│                                                                              │
│ FOLLOWER HEALTH (PERSISTENT):                                                │
│ • XR_KelvinHealth: Kelvin HP multiplier (1x-200x, base 100 HP)              │
│   At 200x = 20,000 HP                                                       │
│ • XR_VirginiaHealth: Virginia HP multiplier (1x-200x, base 120 HP)          │
│   At 200x = 24,000 HP                                                       │
│ • HP persists through first-aid revival (delayed timer reapplies)            │
│ • Applied at spawn via VailActor.OnActorEnabled postfix                      │
│ • Reapplied after revival via VailActor.Revive postfix (1s delay)            │
│                                                                              │
│ MULTIPLAYER SCALING:                                                         │
│ • XR_AdjustByPlayerCount: Auto-scale difficulty with players                │
│ • XR_ExtraSpawnsPerPlayer: Extra spawns per connected player                │
│ • XR_ExtraRaidsPerPlayer: Extra raids per connected player                  │
│ • XR_ExtraBossesPerPlayer: Extra bosses per connected player                │
└──────────────────────────────────────────────────────────────────────────────┘

INTERNAL MECHANICS:
- Uses Harmony Postfix on VailActor.OnEnable() for enemy stat application
- Uses Harmony Postfix on VailActor.Revive() for follower HP persistence
- Uses Harmony Postfix on VailWorldEvents.RunEvent() for raid announcements
  Postfix signature: (VailWorldEventData.EventBase __0, bool __result)
  Only announces SearchPartyEvent when __result==true and player not in caves
- EventAnnouncer.cs handles on-screen messages via SonsTools.ShowMessage
- Follower HP: identifies HealthStat via _baseValue matching (not _max)
  VailActor has 13 stats — stat[12] is HealthStat, stat[0] is behavioral
- Writes three stat fields: _currentValue (0x10), _baseValue (0x14), _max (0x24)
- Revive postfix uses System.Threading.Timer (1s delay) to avoid game HP reset
  race with DyingRecoverHealth recovery logic
- Shared ApplyFollowerHP() helper used by both OnActorEnabled and Revive paths
- Manual Harmony patching (HarmonyPatchAll=false for IL2CPP safety)
- Server-authoritative: Raid scheduling runs on host/server
- Re-queues raids when config changes (MustRequeueRaids flag)

================================================================================
                         11. LOOT RESPAWN MODULE  *** UPDATED ***
================================================================================

BASED ON: GlaDOS (laserman120) — LootRespawnControl v2
  https://github.com/laserman120/SOTF-Mod-LootRespawnControl (AGPL-3.0)

PURPOSE:
Tracks collected loot items, opened containers, and broken crates, allowing
them to respawn after a configurable number of in-game days. All item types
are tracked with unique positional identifiers. After the respawn timer
expires, items re-appear and containers reset when the area reloads.

*** NEW: SERVER-AUTHORITATIVE TRACKING (March 08, 2026) ***
On dedicated servers, clients report ground pickup collections to the server
via Bolt's built-in debugCommand event (silent C→S transport, Pattern #28).
A server-side LootEventListener (GlobalEventListener) receives and processes
these events. Container events (break/open) fire server-side directly — no
C→S transport needed. The server maintains the central tracker for ALL
players and persists to disk.

KEY DIFFERENCES FROM ORIGINAL:
- Integer hash for pickups (original uses MD5 — bottleneck with 600+ Awake calls)
- Standalone JSON persistence (original uses game save system ICustomSaveable)
- Manual Harmony patching (original uses HarmonyPatchAll = true)
- Clone filtering: explicit skip for (Clone) items from containers
- 2 config options vs original's 50+ (per-category allow/block/timed/sync)
- Container respawn (openable + breakable + GrabBag) is ENTIRELY NEW —
  the original mod only handles ground pickups, not containers
- Server-authoritative tracking via debugCommand C→S is ENTIRELY NEW —
  the original mod has no multiplayer/server support

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ This module works AUTOMATICALLY when enabled.                                │
│                                                                              │
│ TO CONFIGURE:                                                                │
│ 1. Go to Settings → Mods → ProjectX - Modules                              │
│ 2. Toggle "Enable Loot Respawn" on/off                                      │
│ 3. Set "Loot Respawn Days" (1-30):                                          │
│    • Default: 3 days                                                        │
│    • Lower = items come back faster                                         │
│    • Higher = more realistic/challenging                                     │
│                                                                              │
│ HOW IT WORKS:                                                                │
│ 1. You pick up any item in the world (supplies, tools, ammo, etc.)         │
│ 2. The mod records WHAT you took and WHERE (via positional hash)           │
│ 3. After the configured number of in-game days, the item can respawn       │
│ 4. On next world load, items past their timer re-appear in the world       │
│                                                                              │
│ HOW IT WORKS (Dedicated Server):                                             │
│ 1. ANY player picks up a ground item → client sends debugCommand event      │
│    (input="px:loot:collect", input2="{hash}:{itemId}")                       │
│ 2. LootEventListener (GlobalEventListener) receives on server               │
│ 3. Server records hash + timestamp + item ID in central tracker             │
│ 4. Container events (break/open) fire server-side directly (no C→S needed) │
│ 5. Server persists tracker to loot_collected.dat via ServerTickPostfix      │
│ 6. On server restart, tracked data loads → containers suppressed:          │
│    • Openable: marked as visually opened via Start hook (isOpen + icon)    │
│    • Openable: PREFIX blocks any interaction attempts (belt & braces)      │
│    • Breakable: destroyed during Awake if timer not expired                │
│    • Pickups: destroyed during Awake if timer not expired                  │
│ 5. When timer expires, containers/items respawn fresh on next load         │
│ 6. Use /px loot status to check: Tracked, Suppressed, Respawned counts    │
│                                                                              │
│ SUPPORTED ITEM TYPES:                                                        │
│ • Ground pickups (food, meds, ammo, materials, rope, etc.)                  │
│ • Openable containers (ammo cases, storage crates, meds, flares)           │
│ • Breakable containers (wooden crates, coffins)                             │
│                                                                              │
│ NOTE:                                                                        │
│ • Items respawn where they originally spawned                               │
│ • Containers re-appear closed/intact and can be opened/broken again        │
│ • Breakable containers spawn with fresh loot on each respawn cycle         │
│ • 1 in-game day ≈ 24 real minutes (default cycle)                          │
│ • At default 3-day respawn: items return after ~72 real minutes             │
│ • Suppressed containers look opened/empty — no player confusion            │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS:
- LootRespawnEnabled (bool): Enable/disable loot respawn tracking
  Default: true
- LootRespawnDays (int): In-game days until items respawn
  Default: 3, Range: 1-100

PER-CATEGORY TOGGLES (Settings → ProjectX - Loot Categories):
All default to true. Disabling a category means those items will NOT be
tracked for respawn — they will always be present in the world.
- LR_TrackMelee: Melee Weapons (Modern Axe, Katana, Chainsaw, etc.)
- LR_TrackRanged: Ranged Weapons (Pistol, Shotgun, Crossbow, etc.)
- LR_TrackWeaponMods: Weapon Mods (Silencer, Laser Sight, etc.)
- LR_TrackMaterials: Crafting Materials (Rope, Duct Tape, Coins, etc.)
- LR_TrackFood: Food (MREs, Canned Food, Cereal, etc.)
- LR_TrackMeds: Medicine & Energy (Pills, Candy Bars, Energy Drinks)
- LR_TrackPlants: Plants (Aloe Vera, Mushrooms, Chicory, etc.)
- LR_TrackAmmo: Ammunition (Bullets, Arrows, Bolts, Ziplines)
- LR_TrackThrowables: Throwables (Grenades, Sticky Bombs, Golf Balls)
- LR_TrackExpendables: Expendables (Printer Resin, Air Tanks, Hide Bags)
- LR_TrackBreakables: Breakable Containers (Wooden Crates, Coffins)
- LR_TrackOpenables: Openable Containers (Ammo Cases, Pelican Cases, Suitcases)

INTERNAL MECHANICS:

Ground Pickups:
- Harmony Postfix on PickUp.Awake(): Checks suppression table on spawn
- Harmony Postfix on PickUp.Collect(): Records collection with timestamp
- Identifier: Integer hash combining (name.GetHashCode() + quantized position)
  Previously used MD5 — replaced for performance (600+ PickUp.Awake calls
  during world load made MD5.Create()+ComputeHash() a bottleneck)
- Early-exit guard: if suppression dictionary is empty, Awake hook is no-op

Openable Containers:
- Harmony PREFIX on ContainerItemSpawner.OpenContainer(): Blocks streaming
  replays (spawnItems=false) for expired entries in _collected
- Harmony POSTFIX on ContainerItemSpawner.OpenContainer(): Records fresh
  opens during gameplay
  • LOCAL PLAY: tracks spawnItems=False call (player opened signal)
  • DEDICATED SERVER: tracks spawnItems=True + contentsSeed>0 + saveDataLoaded
    (On servers, OpenContainer always fires with spawnItems=True for player opens.
     The spawnItems=False calls only happen during deserialization/streaming.)
- Does NOT call ClearStateSync (breaks container interaction)

Breakable Containers:
- Harmony PREFIX on BreakableObject.OnBreak(): Frame-based blocking
  (≤120 frames of Awake) + expired timer fallback from _collected
- Harmony POSTFIX on BreakableObject.OnBreak(): Records break events
- ClearContainerSaveState(): Finds child ContainerItemSpawner, calls
  ClearStateSync() + set__isOpen(false) for fresh loot contentsSeed
- _breakableLoadFrame dictionary tracks frame count per breakable hash
- OnBreak fires on the dedicated server (via Harmony) — server records directly

Shared:
- Timer: RespawnDays × 1440 seconds (1 in-game day ≈ 1440 seconds)
- _collected entries persist permanently (never removed — needed for PREFIX)
- _recentlyRespawned HashSet prevents re-recording during streaming
- Manual Harmony patching (HarmonyPatchAll=false for IL2CPP safety)
- Runs on all deployment tiers (Owner, Server, Client)

Dedicated Server Lifecycle Fix (Phase 344):
  On dedicated servers, OnSdkInitialized does NOT fire, so Init() runs inside
  OnGameStart() — AFTER OnGameStarted() has already set _saveDataLoaded=true.
  Init() unconditionally reset _saveDataLoaded=false, breaking all tracking.
  Fix: Init() preserves _saveDataLoaded if already true. OnSceneInit() also
  skips reset if _saveDataLoaded is already true (prevents post-game scene
  inits from resetting the tracker).

Dual-Path Architecture (March 08, 2026):
  Ground pickup collection uses a runtime branch:
  - BoltNetwork.isServer or !BoltNetwork.isRunning → record locally (existing)
  - IsMultiplayerClient() → send debugCommand.Raise(GlobalTargets.Everyone)
    with input="px:loot:collect" and input2="{hash}:{itemId}" (NEW)
  Container events (OnBreak, OpenContainer) fire server-side directly —
  no C→S transport needed for these.
  Local host behavior is IDENTICAL to before — the debugCommand path only
  activates when running as a client on a dedicated server.

Silent C→S Transport (debugCommand + LootEventListener):
- Uses Bolt's built-in debugCommand event (from bolt.user.dll)
- Pattern proven by SimpleNetworkEvents (7,800+ installs), Shops, Banking mods
- LootEventListener: [RegisterTypeInIl2Cpp] GlobalEventListener subclass
  • Start() calls BoltNetwork.AddGlobalEventListener() — REQUIRED
  • OnEvent(debugCommand) override — must NOT have [HideFromIl2Cpp]
  • Filters by BoltNetwork.isServer + evnt.input == "px:loot:collect"
  • Parses evnt.input2 as "{hash}:{itemId}" and calls RecordPickupFromClient()
- Client sends via: debugCommand.Raise(Bolt.GlobalTargets.Everyone)
  (NOT OnlyServer — Bolt security blocks OnlyServer for built-in events)
- Namespace note: using Bolt; conflicts with System.Math — fully qualify
  Bolt types or alias Math

================================================================================
                       12. DEDICATED SUPERUSER MODULE
================================================================================

PURPOSE:
Provides admin chat commands and server-side command execution for dedicated
server hosts. Owner clients route commands to the server via ChatBox.SendLine.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ Type commands in the in-game chat. All commands use the /px prefix.         │
│                                                                              │
│ COMMANDS:                                                                    │
│                                                                              │
│ WORLD COMMANDS:                                                              │
│ • /px world time [0-24]        → Set time of day                            │
│ • /px world season [name]      → Change season (spring/summer/autumn/winter)│
│ • /px world weather [type]     → Set weather (sunny/rain/snow)              │
│ • /px world freeze             → Toggle AI freeze                           │
│ • /px world treeregrow         → Regrow all trees                           │
│ • /px world revivekelvin       → Revive Kelvin                              │
│ • /px world revivevirginia     → Revive Virginia                            │
│                                                                              │
│ RAID COMMANDS:                                                               │
│ • /px raid status              → Show queued raid events                    │
│ • /px raid start               → Start a random raid                        │
│ • /px raid clear               → Clear all queued raids                     │
│ • /px raid requeue             → Requeue all raid events                    │
│                                                                              │
│ CONFIG COMMANDS:                                                             │
│ • /px config get [key]         → Read config value                          │
│ • /px config set [key] [val]   → Set config value                           │
│ • /px config save              → Save config to disk                        │
│ • /px config list              → List all config entries                    │
│                                                                              │
│ SERVER COMMANDS:                                                             │
│ • /px save                     → Force world save                           │
│ • /px status                   → Server status info                         │
│                                                                              │
│ OWNER GUI (Server Admin panel):                                              │
│ The Owner edition GUI has a "Server Admin" panel with buttons that route    │
│ commands via ServerCmd() → ChatBox.SendLine → /px chat hook on server.      │
└──────────────────────────────────────────────────────────────────────────────┘

COMMAND ROUTING:
- Owner/Client → ChatBox.SendLine("/px ...") → server chat hook intercepts
- Server → CommandBridge.ProcessMessage() dispatches to handlers
- Local fallback: if player IS the server, executes locally

INTERNAL MECHANICS:
- Chat hook via Harmony Postfix on ChatBox (multi-class fallback)
- CommandBridge routes /px commands to registered handler modules
- RaidCommands.cs, WorldCommands.cs, ConfigCommands.cs, LootCommands.cs,
  ServerCommands.cs — separate handler files per command category
- WorldCommands uses TrySendDebugCommand() for server-authoritative actions
- Server-safe tick driver: SeasonsManager.LateUpdate Harmony Postfix

================================================================================
                         13. BUILDING CHEATS MODULE
================================================================================

PURPOSE:
Provides building-related cheats and enhancements via IMGUI toggles.
All features are accessible from the Project X menu under "Building Cheats".

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ 1. Open the Project X menu (default: Insert key)                            │
│ 2. Scroll to the "BUILDING CHEATS" section                                  │
│ 3. Toggle features on/off:                                                  │
│                                                                              │
│ AVAILABLE TOGGLES:                                                           │
│ • Free Form Placement - Place structures anywhere without snapping          │
│   Uses: GameSetupManager.SetFreeFormForcePlaceFullLoadSetting(bool)         │
│                                                                              │
│ • Instant Build - Structures complete instantly after placement             │
│   On dedicated server: polls finishblueprints every 0.5s (server-side)     │
│   On SP/P2P: Harmony PREFIX on StructureCraftingSystem.Update              │
│                                                                              │
│ • No Wood Cuttings - Structures build without wood cutting debris           │
│   Uses: GameSetupManager.SetConstructionSkipCuttingsSpawnSetting(bool)      │
│                                                                              │
│ • Log Hack - Infinite log carrying                                          │
│   Uses: DebugConsole.Instance._loghack("on"/"off")                          │
│                                                                              │
│ • Stone Hack - Infinite stone carrying                                      │
│   Uses: DebugConsole.SendCommand("stonehack on/off")                        │
│                                                                              │
│ NOTE: Free Form and No Wood Cuttings use direct game APIs, NOT console      │
│ commands. These were previously broken (fake commands that didn't exist).    │
│ Fixed Feb 08 2026 by discovering the real GameSetupManager API in dump.cs.  │
└──────────────────────────────────────────────────────────────────────────────┘

INTERNAL MECHANICS:
- BuilderEnhancements.cs contains all toggle logic
- FreeForm and NoCuttings: Direct static API calls on
  Sons.Gameplay.GameSetup.GameSetupManager (requires Sons.Gameplay.GameSetup.dll)
- Instant Build (Dedicated Server): ServerTickPostfix polls "finishblueprints"
  every 0.5s via WorldCommands.TrySendDebugCommand (server-authoritative)
- Instant Build (SP/P2P): Harmony PREFIX on StructureCraftingSystem.Update
  writes InstantBuild=true at offset 0x71 every frame

API DISCOVERY NOTES:
- "skipwoodcuttings" and "freeformplace" console commands DO NOT EXIST
- Real APIs found in dump.cs under GameSetupManager class (line 1187840+)
- GameSetupManager is in namespace Sons.Gameplay.GameSetup
- Required adding Sons.Gameplay.GameSetup.dll as a project reference

================================================================================
                             14. AMMO UI MODULE
================================================================================

PURPOSE:
Displays a HUD overlay showing current ammo count when holding a ranged weapon.
Based on the standalone AmmoUi mod by Axel, now integrated into Project X with
configurable size and opacity.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ This module works AUTOMATICALLY.                                             │
│                                                                              │
│ 1. Equip any ranged weapon (guns, crossbow, etc.)                           │
│ 2. The ammo counter appears at the bottom-left of the screen:              │
│    • Large number = remaining ammo in weapon                                │
│    • Smaller number = total ammo in inventory                               │
│    • Weapon icon displayed alongside                                        │
│ 3. The display auto-hides when:                                             │
│    • Not holding a ranged weapon                                            │
│    • In pause menu                                                          │
│    • In inventory screen                                                    │
│                                                                              │
│ CUSTOMIZATION:                                                               │
│ Go to Settings → Mods → ProjectX - AmmoUI:                                  │
│ • AmmoUI Size (0.1 - 2.0): Scale of the display (default: 1.0)             │
│   - 0.5 = half size, 1.0 = normal, 2.0 = double size                      │
│ • AmmoUI Opacity (0 - 1.0): Transparency (default: 1.0)                    │
│   - 0 = invisible, 0.5 = semi-transparent, 1.0 = fully opaque             │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS (Settings → Mods → ProjectX - AmmoUI):
- AmmoUiSize (float, 0.1-2.0): Scale multiplier for the panel
  Default: 1.0
  Applied via RectTransform.localScale (same as original mod)
- AmmoUiOpacity (float, 0-1.0): Alpha for icon and text color
  Default: 1.0

INTERNAL MECHANICS:
- SUI panel: 350×120 pixels base size, AnchorType.BottomLeft
- Text uses TMP rich text: <size=36> for remaining, <size=28> for total
- Weapon detection: rightHandItem.Data._weaponType == 2 (ranged)
- Ammo counting via reflection: LocalPlayer.Inventory.AmountOf()
- Harmony Postfix on RangedWeapon.SetBulletsAmount for real-time updates
- Configurable at init time — changes require game restart

================================================================================
                         16. DISCORD BRIDGE MODULE
================================================================================

PURPOSE:
Bridges in-game events (player joins, deaths, chat) to a Discord channel via
webhook or bot integration. Also provides an in-game chat overlay.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ SETUP:                                                                       │
│ 1. Open the Project X menu (Insert key)                                     │
│ 2. Navigate to the "Discord" tab (6th tab)                                  │
│ 3. Enable "Discord Bridge"                                                  │
│ 4. Configure bot token and channel ID in native settings:                   │
│    Settings → Mods → ProjectX - Discord                                     │
│                                                                              │
│ TEST BUTTONS (Discord tab):                                                  │
│ • Test Message - Send a test message to Discord channel                     │
│ • Test Welcome - Send a welcome embed                                       │
│ • Test Join - Simulate player join event                                    │
│ • Test Leave - Simulate player leave event                                  │
│ • Test Death - Simulate player death event                                  │
│                                                                              │
│ IN-GAME CHAT:                                                                │
│ • Test Chat - Test the in-game chat overlay                                 │
│ • Test Welcome - Send a welcome message for "TestPlayer"                    │
└──────────────────────────────────────────────────────────────────────────────┘

CONFIG OPTIONS (Settings → Mods → ProjectX - Discord):
- EnableDiscordBridge (bool): Master toggle. Default: false
- DiscordBotToken (string): Bot authentication token
- DiscordChannelId (string): Target Discord channel ID

INTERNAL MECHANICS:
- BroadcastMessageModule handles Discord webhook/bot communication
- InGameChat handles the in-game chat overlay
- Client-only module (#if !SERVER)
- ProtectFromReset removed (March 2026) — Discord config entries are
  in their own category, so native "Reset to Default" cannot wipe them.
  The old ProtectFromReset caused infinite recursion crashes via
  OnValueChanged re-entrancy.

================================================================================
                     17. GUI LAYOUT & MENU SYSTEM
================================================================================

PURPOSE:
The Project X GUI is the main in-game control panel for all modules.
Uses Unity IMGUI (not SUI) for IL2CPP stability.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ OPENING THE MENU:                                                            │
│ Press the Insert key (configurable in Settings → Mods → ProjectX - Keys)    │
│ NOTE: The Insert key is BLOCKED when the Esc pause menu is active.           │
│ This prevents the mod menu overlapping with the native pause menu.           │
│                                                                              │
│ TABS (7 total):                                                              │
│ 1. Player      - Cheats (god mode, inf stamina, max strength, etc.),        │
│                   movement sliders (with reset toggle), enemy actions        │
│ 2. Environment  - Terrain toggles (No Grass, No Forest),                    │
│                   seasons (Spring/Summer/Autumn/Winter toggles),             │
│                   time of day, daytime speed, tree/wind/waterfall sliders,  │
│                   force rain, no world gravity, loot respawn settings       │
│ 3. Teleport     - Quick teleport, coordinate input, location buttons,       │
│                   COMPANIONS: unstuck, revive, virginia visit/happy,        │
│                   HP multiplier sliders, stat override toggle               │
│ 4. Building+    - Module toggles (Relocator, Durability, etc.)              │
│ 5. Raids        - X Raids scheduling, enemy types, stat multipliers,        │
│                   raid announcements, AI aggression slider (aiangerlevel)   │
│ 6. Server Admin - Admin commands for dedicated server control               │
│ 7. Server Raids - Server-side raid configuration                            │
│                                                                              │
│ NAVIGATION:                                                                  │
│ Use the ← → arrow buttons at the top to switch between tabs.               │
│ All panels are scrollable for content that exceeds the visible area.        │
│                                                                              │
│ SETTINGS AUTO-APPLY:                                                         │
│ All toggle and slider changes take effect immediately — no save button      │
│ needed. Config values are persisted to ProjectX.Master.cfg automatically.   │
└──────────────────────────────────────────────────────────────────────────────┘

LAYOUT DIMENSIONS:
- Panel: 1100 × 1150 pixels (positioned top-left, ends above Hotbar)
- Selector Bar: 800 × 65 pixels (tab navigation)
- Panel starts at Y=100, leaving room for Hotbar at bottom
- Red/black theme with glassmorphism-style backgrounds

STYLING (ProjectXStyles.cs):
- Title: 34px bold, AccentRed
- Headers: 26px bold, white
- Dividers: 22px bold, AccentRed ("═══ SECTION ═══" format)
- Labels: 20px, white
- Buttons: 20px, 45px height, 16px horizontal padding
- Toggles: 20px, AccentRed when active
- Color scheme: PanelColor (deep black 0.97α), ButtonColor (dark red-black),
  ButtonHoverColor (red), AccentRed (0.85, 0.15, 0.15)

================================================================================
                             RECIPE ID REFERENCE
================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│ Recipe ID │ Structure               │ Used By Module                         │
├───────────┼─────────────────────────┼────────────────────────────────────────┤
│ 19        │ Drying Rack             │ Meat Dryer Module                      │
│ 56        │ Rain Catcher            │ Water Collectors Module                │
│ 71        │ Cross                   │ Scary Cross Module                     │
│ 403       │ Standing Fire (Profile) │ Scary Cross Module (fire effects)     │
│ 468       │ Cross (Held Item)       │ Scary Cross Module                     │
└──────────────────────────────────────────────────────────────────────────────┘

================================================================================
                            SEASON VALUE REFERENCE
================================================================================

┌───────────────────────────────────────────────────────────────┐
│ Value │ Season │ Notes                                       │
├───────┼────────┼─────────────────────────────────────────────┤
│ 0     │ Spring │ Best for meat drying (30x boost)            │
│ 1     │ Summer │ Good for drying, prevents water freezing    │
│ 2     │ Autumn │ Moderate drying boost (15x)                 │
│ 3     │ Winter │ Water collectors freeze, poor drying (5x)   │
└───────────────────────────────────────────────────────────────┘

================================================================================
                    15. BUILDER STACKS MODULE  *** NEW ***
================================================================================

PURPOSE:
Carry more logs, planks, and stones beyond vanilla limits using a virtual buffer.
Based on ItemCarryAmount 1.0.0 by mΔ, rewritten for IL2CPP with per-material
capacity, native visual stacking, and configurable limits.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ 1. Go to Settings → Mods → Project X → Builder Stacks                       │
│ 2. Enable Builder Stacks (toggle on)                                        │
│ 3. Set Max Log Capacity, Max Plank Capacity, Max Stone Capacity             │
│    (type exact numbers — e.g. "4", "8", "12")                               │
│ 4. Enable Max Limit must be ON for the cap to take effect                   │
│ 5. Pick up materials normally — the mod buffers excess items invisibly      │
│ 6. When you place one, the next item is given back from the buffer          │
│ 7. A HUD panel shows the material icon + carry count (e.g. "4/8")       │
└──────────────────────────────────────────────────────────────────────────────┘

HOW IT WORKS:

When ENABLED with max limit ON:
  - Player picks up materials normally. Game shows vanilla visual stacking
    (logs stack to 2 on shoulder, planks/stones stack to 4)
  - When hands reach the native hold limit, excess items are absorbed into
    an invisible buffer and _heldCount is reset
  - Total carry = buffer + native hold limit = configured max capacity
  - When the player places an item and hands become empty, the mod waits
    a short delay then gives one back from the buffer via TryEquip
  - At capacity: mod does nothing, game enforces native hold limit

When ENABLED with max limit OFF:
  - No cap — carry unlimited materials

When DISABLED:
  - Reverts to vanilla carry limits (2 logs, 4 planks, 4 stones)

NATIVE HOLD LIMITS (vanilla game):
┌───────────────────────────────────────────────────────────────┐
│ Material │ Native Hold │ Visual Stack │ Notes                 │
├───────────────────────────────────────────────────────────────┤
│ Logs     │ 2           │ 2 on shoulder│ IDs: 78, 406, 408, 409│
│ Planks   │ 4           │ 4 on shoulder│ IDs: 395, 576-578     │
│ Stones   │ 4           │ 4 on shoulder│ ID: 640               │
└───────────────────────────────────────────────────────────────┘

CONFIGURATION:
┌─────────────────────────────────────────────────────────────────────────┐
│ Setting              │ Default │ Type   │ Description                   │
├─────────────────────────────────────────────────────────────────────────┤
│ Enable Builder Stacks│ true    │ bool   │ Master toggle for the module  │
│ Max Log Capacity     │ "2"     │ string │ Max logs (vanilla: 2)         │
│ Max Plank Capacity   │ "4"     │ string │ Max planks (vanilla: 4)       │
│ Max Stone Capacity   │ "4"     │ string │ Max stones (vanilla: 4)       │
│ Enable Max Limit     │ true    │ bool   │ Enforce carry cap             │
│ Re-Equip Delay       │ 0.20    │ float  │ Seconds between give-backs    │
└─────────────────────────────────────────────────────────────────────────┘

Note: Capacity settings use text entry fields (not sliders) for exact values.
There is no upper limit — set any integer value you want.

FILES:
 - Modules/BuilderStacks/BuilderStacksModule.cs  (buffer logic, _heldCount write)
 - Config.cs  (string entries with int.TryParse, OnValueChanged subscriptions)

IL2CPP NOTES:
 - Cast<HeldOnlyItemController>()._heldCount → stripped. Replaced with direct
   memory write via Pointer + offset 0x5C (Marshal.WriteInt32).
 - Separate buffers per material type (log, plank, stone).
 - Building materials excluded from LootRespawn tracking to prevent interference.

================================================================================
                        18. AUDIO CONTROL MODULE  *** NEW ***
================================================================================

PURPOSE:
Controls the volume of waterfall sounds using FMOD event emitters.
Based on WaterfallSoundControl 1.0.0 by Toni Macaroni, rewritten for IL2CPP.

┌──────────────────────────────────────────────────────────────────────────────┐
│ HOW TO USE                                                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ 1. Open Project X menu → Environment tab → Waterfall Volume slider          │
│ 2. OR go to Settings → Mods → Project X → Features → Waterfall Volume       │
│ 3. Adjust slider: 0.0 = mute, 1.0 = default, 3.0 = 3x volume              │
│ 4. Changes apply instantly — no restart needed                              │
└──────────────────────────────────────────────────────────────────────────────┘

HOW IT WORKS:

The original mod uses Scene.GetRootGameObjects() + GetComponentsInChildren
to find waterfall emitters — both are stripped by IL2CPP.

Project X uses Harmony Postfix on SonsFMODEventEmitter.Awake to track FMOD
event emitters as the scene loads. Each emitter is checked: if its root
transform parent is named "WaterfallSounds" (in the Site02StreamsAndLakes
scene), it is added to the tracking list.

Volume is applied via SetVolume() + Stop/Play restart cycle, which forces
FMOD to pick up the new volume value immediately.

CONFIGURATION:
┌─────────────────────────────────────────────────────────────────────────┐
│ Setting              │ Default │ Range     │ Description               │
├─────────────────────────────────────────────────────────────────────────┤
│ WaterfallVolume       │ 1.0     │ 0.0 - 3.0 │ Volume multiplier         │
└─────────────────────────────────────────────────────────────────────────┘

FILES:
 - Modules/Audio/AudioControl.cs  (FMOD Harmony tracking + volume control)

IL2CPP NOTES:
 - Scene.GetRootGameObjects() → stripped. Replaced with Harmony Awake tracking.
 - GetComponentsInChildren<SonsFMODEventEmitter>() → stripped (generic plural).
   Replaced with per-emitter Awake tracking + root parent name filtering.
 - _baseVolume field → private in DummyDll. Use SetVolume() public method.

================================================================================
                    REMOVED MODULES (Historical Reference)
================================================================================

The following modules have been permanently removed from Project X due to
IL2CPP compatibility issues. They are documented here for historical context.

OPEN SESAME:
- Allowed doors to be opened from either side regardless of lock direction
- Removed due to HarmonyPatchAll vtable corruption causing backpack crashes
- Harmony Postfix on DoorLock.IsInFront() forced __result = true

PREFAB REPAIR:
- Allowed repairing damaged structures with the repair tool
- Removed due to HarmonyPatchAll vtable corruption causing backpack crashes
- Modified ScrewStructureDestruction._currentHp

STONE GATE:
- Added craftable stone gates with open/close mechanics
- Removed due to ClassInjector type registrations destabilizing inventory UI
- Custom Item ID: 751152, used ClassInjector.RegisterTypeInIl2Cpp<T>()
- Has known upstream crash bug (confirmed by original mod author)

================================================================================
                              END OF DOCUMENT
================================================================================
